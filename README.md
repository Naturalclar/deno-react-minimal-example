# Example Deno server with React

a minimal example of `Deno` server with `React`

## Running server

```sh
deno ./server.tsx
```

then access `localhost:6796` in your browser

## with Docker Compose

run following command in your terminal

```sh
docker-compose up
```

then access `localhost:6796` in your browser